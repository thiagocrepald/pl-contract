CREATE TABLE `MOBILE_VERSION` (
                                `ID` int(11) NOT NULL AUTO_INCREMENT,
                                `ANDROID_VERSION` varchar(50) DEFAULT NULL,
                                `IOS_VERSION` varchar(50) DEFAULT NULL,
                                PRIMARY KEY (`ID`)
);